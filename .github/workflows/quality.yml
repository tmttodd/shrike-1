name: Quality Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Run tests
        run: python -m pytest tests/unit/ tests/test_server.py tests/test_config.py -q

      - name: Upgrade pip (suppress known CVE in runner)
        run: pip install --upgrade pip

      - name: Vulnerability scan
        run: pip install pip-audit && pip-audit --ignore-vuln CVE-2026-3219
