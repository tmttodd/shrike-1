source: trellix_epo
description: 'Auto-generated from Splunkbase TA: ta_trellix_epo'
version: 1
auto_generated: true
patterns:
- name: ta_trellix_epo_threat_fields_threat_extractions
  match:
    log_format:
    - syslog_bsd
    - syslog_rfc5424
    - syslog_rfc3164
    - kv
    - custom
    regex: '"detectionId"\s*:\s*"?(?P<detection_id>[^",}]+)"?.*?"threatName"\s*:\s*"(?P<threat_name>[^"]+)".*?"severity"\s*:\s*"(?P<severity>[^"]+)"'
  ocsf_class_uid: 2004
  ocsf_class_name: Detection Finding
  static:
    severity_id: 1
    category_uid: 2
    category_name: Findings
  field_map:
    severity: severity_id
- name: ta_trellix_epo_malware_fields_malware_extractions
  match:
    log_format:
    - syslog_bsd
    - syslog_rfc5424
    - syslog_rfc3164
    - kv
    - custom
    regex: '"malwareName"\s*:\s*"(?P<malware_name>[^"]+)".*?"filePath"\s*:\s*"(?P<file_path>[^"]+)".*?"fileHash"\s*:\s*"(?P<file_hash>[^"]+)"'
  ocsf_class_uid: 2004
  ocsf_class_name: Detection Finding
  static:
    severity_id: 1
    category_uid: 2
    category_name: Findings
  field_map:
    file_path: process.file.path
    file_hash: process.file.hashes.value
